<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.dao.PersonalcenterDao">

    <update id="addUserXinxi" parameterType="com.jk.model.User">
      update user u,t_aptitude ap set ap.name=#{name} where u.userInfoid= ap.id
      and u.userId=#{userId}
    </update>

</mapper>